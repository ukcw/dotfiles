- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.tmux.conf: tmux/tmux.conf
    ~/.vimrc: .vimrc
    ~/.config/nvim: 
      force: true
      path: nvim

#- create:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
